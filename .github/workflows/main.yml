name: Standalone CLI
on: [push, pull_request]

jobs:
  machineStandalone:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run Skims
        uses: docker://docker.io/fluidattacks/skims:latest
        with:
          args: skims --strict --debug scan .
